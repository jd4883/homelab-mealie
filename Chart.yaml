# Instantiation: base chart from expectedbehaviors/mealie; override in values.yaml (mealie.*, onepassworditem.*).
# Run: helm repo add mealie https://expectedbehaviors.github.io/mealie && helm repo update
apiVersion: v2
name: mealie
description: Mealie recipe manager — recipes, meal planning, shopping lists. Base: expectedbehaviors/mealie; OIDC via nginx.
version: 0.1.0
appVersion: "1.4.0"
kubeVersion: ">=1.22.0-0"
keywords:
  - mealie
  - recipe
  - meal-planning
home: https://mealie.io
sources:
  - https://github.com/mealie-recipes/mealie
  - https://github.com/expectedbehaviors/mealie
maintainers: []
dependencies:
  - name: mealie
    version: "1.0.0"
    repository: https://expectedbehaviors.github.io/mealie
